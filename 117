#include<stdio.h>
int main()
{
	int n,m,b[100][100];
	while(scanf("%d%d", &n, &m)==2)
	{
		int flag=1;
		for(int k=0;k<n;k++)
		{for(int i=0;i<m;i++)
			{b[k][i]=flag++;
			 if(i!=m-1)printf("%d ", b[k][i]);
			 else
				 printf("%d\n", b[k][i]);
		        }
		}
	}
	return 0;
}


				
